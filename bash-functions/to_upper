# Purpose: 
# Converts characters in a string from lower-case to upper-case
#
# Arguments: 
# Strings may be passed as arguments or via stdin

to_upper() {
  if [ $# -gt 0 ]; then
    tr '[a-z]' '[A-Z]' <<<"${@}"
  else
    tr '[a-z]' '[A-Z]'
  fi
}
